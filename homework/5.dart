import 'package:flutter/material.dart';

class assign5 extends StatelessWidget {
  const assign5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(title: Text("assign5"),));
  }
}

