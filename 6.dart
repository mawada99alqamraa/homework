import 'package:flutter/material.dart';

class assign6 extends StatelessWidget {
  const assign6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(title: Text("assign6"),));
  }
}

